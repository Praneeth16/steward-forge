resources:
  experiments:
    model_traces:
      name: ${var.model_trace_experiment_name}
      permissions:
        - level: CAN_READ
          group_name: ${var.auditor_group_name}
        - level: CAN_EDIT
          service_principal_name: ${resources.apps.workbench.service_principal_client_id}
